import 'package:flutter/material.dart';

import 'jobs_builder.dart';

class JobsBody extends StatelessWidget {
  const JobsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const JobsBuilder();
  }
}
